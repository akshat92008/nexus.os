name: nexus-os-api
services:
  - name: api
    type: worker
    build:
      command: pnpm install && pnpm build
      workdir: apps/api
    run:
      command: pnpm start
      workdir: apps/api
    env:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3006
